# Set this to the live domain at deploy time.
# It drives canonical URLs, Open Graph tags, robots.txt and sitemap.xml.
# Locally it falls back to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
